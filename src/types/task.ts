/**
 * Definition of task props
 * @prop {string} name - The name of the task
 * @prop {boolean} completed - Set if the task is completed or not
 */
interface Task {
  name: string;
  completed: boolean;
}

export default Task
